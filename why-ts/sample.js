

// @ts-check

// typescript를 사용하지 않고도 체크할 수 있지만 가독성이 떨어지고 코드량이 늘어나므로 ts를 사용하는 것이 좋다

/**
 * @param {Number} a 
 * @param {Number} b 
 */

function sum(a, b) {
    return a + b;
}

var result = sum(10, 20);


